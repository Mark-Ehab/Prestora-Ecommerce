import { CateogriesFilterPipe } from './cateogries-filter-pipe';

describe('CateogriesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CateogriesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
